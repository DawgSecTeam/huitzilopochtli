{
  "name": "sensitive-file-win",
  "description": "Practice-box helper (Windows): writes $FILE_PATH with $FILE_CONTENT (normal ACLs -- the finding is the file itself, e.g. leftover credentials). Vars: FILE_PATH (required); FILE_CONTENT (optional). Idempotent.",
  "platform": "windows",
  "category": "misconfiguration",
  "type": "powershell",
  "run_as": "Administrator",
  "script": "if (-not $FILE_PATH) { throw 'FILE_PATH is required' }\n$dir = Split-Path $FILE_PATH -Parent\nNew-Item -ItemType Directory -Force -Path $dir | Out-Null\nif ($FILE_CONTENT) { Set-Content -Path $FILE_PATH -Value $FILE_CONTENT -Encoding ascii }\n",
  "depends_on": []
}
