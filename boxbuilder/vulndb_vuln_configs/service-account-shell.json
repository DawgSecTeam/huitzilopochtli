{
  "name": "service-account-shell",
  "description": "Practice-box account hardening finding: creates a service identity with an interactive Bash login shell instead of nologin.",
  "platform": "linux",
  "category": "misconfiguration",
  "type": "command",
  "run_as": "root",
  "script": "#!/bin/sh\nset -eu\n\nif ! id chocobot >/dev/null 2>&1; then useradd -r -m -s /bin/bash chocobot; else usermod -s /bin/bash chocobot; fi\n",
  "depends_on": []
}
